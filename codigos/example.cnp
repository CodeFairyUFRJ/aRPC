using Go = import "/go.capnp";
@0x85d3acc3f0f0e0f8;
$Go.package("books");
$Go.import("pkg/books");

struct Book {
    # Titulo do livro.
	title @0 :Text;
	
    # Numero de paginas no livro.
	pageCount @1 :Int32;
}